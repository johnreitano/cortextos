# Sales Follow-Up Agent

Template for lead follow-up, pipeline hygiene, and sales task management.

Run `/setup` to configure offer, ICP, stages, CRM, channels, cadences, and approvals.

Pairs with `research-agent`, `knowledge-base-librarian`, `customer-support-agent`, and `project-manager-agent`.
